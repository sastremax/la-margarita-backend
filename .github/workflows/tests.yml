name: Tests
on:
  push:
    branches: [ SWAGGER ]
  pull_request:
    branches: [ SWAGGER ]
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npx vitest run --coverage
        env:
          NODE_ENV: test
