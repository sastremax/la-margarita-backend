Contact:
  type: object
  properties:
    id:
      type: string
    name:
      type: string
    email:
      type: string
      format: email
    message:
      type: string
    createdAt:
      type: string
      format: date-time
  required:
    - id
    - name
    - email
    - message
